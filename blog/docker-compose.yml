services:
  blog-neo4j:
    image: neo4j:5
    container_name: blog-neo4j
    restart: unless-stopped
    ports:
      - "${NEO4J_HTTP_PORT:-7475}:7474"
      - "${NEO4J_BOLT_PORT:-7688}:7687"
    environment:
      NEO4J_AUTH: "${NEO4J_USERNAME:-neo4j}/${NEO4J_PASSWORD:-password123}"
    volumes:
      - blog_neo4j_data:/data
      - blog_neo4j_logs:/logs
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:7474 >/dev/null 2>&1 || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 20s

volumes:
  blog_neo4j_data:
  blog_neo4j_logs:
